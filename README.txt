Configuration :

Pour configuer le chemin d'accès des fichiers JSON,
Déposer les fichiers dans le serveur local comme par exemple dans votre répertoire WWW,

Puis dans le répertoire : config > config.js, veuillez spécifier le chemin d'accès
sous la forme 'http://localhost/survey_results_WE.json' par exemple.

